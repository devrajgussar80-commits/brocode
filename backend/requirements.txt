fastapi==0.139.0
uvicorn==0.51.0
cryptography==46.0.6
python-dotenv==1.2.2
# Postgres driver. The [binary] extra ships prebuilt libpq wheels — the plain
# package would try to compile against libpq headers, which python:3.12-slim
# does not have.
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
# Imported directly by app.py for the request models. FastAPI pulls it in too,
# but the range is stated so the dependency is explicit rather than incidental.
pydantic>=2.7,<3
